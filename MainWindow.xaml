<Window x:Class="DataBindingExercise.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="MainWindow" Height="400" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="10" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBox Name="TextBox1" Width="300" Height="150" TextWrapping="Wrap"
                 Text="{Binding ExampleString}"/>
        <TextBox Name="TextBox2" Width="300" Height="150" TextWrapping="Wrap" Grid.Row="2" 
                 Text="{Binding ExampleString}" />
    </Grid>
</Window>